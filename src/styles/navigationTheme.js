import colors from "./colors";

const navigationTheme = {
  dark: false,
  colors: {
    primary: colors.light,
    background: colors.primary.dark,
    card: colors.secondary.dark,
    text: colors.light,
    border: colors.light,
  },
};

export default navigationTheme;
