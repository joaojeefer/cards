const config = {
  url: {
    baseURL: "https://api-scrapbook.herokuapp.com",
    signIn: "/login",
    createAccount: "/users",
    getCards: "/cards",
    cardCreate: "/cards",
  },
};

export default config;
